# Data

기본 실행은 Yahoo Finance를 통해 데이터를 내려받으므로 데이터 파일이 필요하지 않습니다.
직접 CSV를 사용할 경우 `Date`와 `Close` 열을 포함한 파일을 이 폴더에 둘 수 있습니다.

예:

```csv
Date,Close
2025-01-02,5900.00
2025-01-03,5925.25
```

원시 시장 데이터의 재배포 권한은 데이터 제공자의 이용조건에 따라 별도로 확인해야 합니다.
